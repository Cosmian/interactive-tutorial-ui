import { callbacksExamplesInMemory } from "cloudproof_js";

//
// Define your callbacks or use basic in memory examples
//
export const { fetchEntries, fetchChains, upsertEntries, insertChains } = callbacksExamplesInMemory();
